# OpenRouter — closed-model dispatch (Claude, GPT, Gemini)
OPENROUTER_API_KEY=

# Groq — open-model dispatch (Llama). Free tier sufficient for v0.
GROQ_API_KEY=

# HuggingFace — PL-specific (Bielik). Free tier; expect rate limits.
HF_TOKEN=
